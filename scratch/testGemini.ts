import { GoogleGenAI } from '@google/genai'; const ai = new GoogleGenAI({ apiKey: process.env.VITE_GEMINI_API_KEY }); ai.models.generateContent({ model: 'gemini-flash-latest', contents: 'hi', config: { tools: [{ functionDeclarations: [{ name: 'test', description: 'test', parameters: { type: 'OBJECT', properties: { q: { type: 'STRING', description: 'test' } }, required: ['q'] } }] }] } }).then(console.log).catch(e => console.log('ERROR:', e));
